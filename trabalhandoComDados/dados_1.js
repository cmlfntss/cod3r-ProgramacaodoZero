console.log("Caneta");
console.log(10); //Os valores diretos são chamados de literal
console.log(6.4);
console.log(1.5);
console.log(7.9);

// na nossa linguagem de programação criamos variáveis para armazenar os dados, com o var
// variaveis são espaços de memórias que armazenamos valores, como exemplo:

var quantidade = 10;
var pencil = "Caneta";
var preco = 6.4;
var imposto = 1.5;
var precoFinal = preco + imposto;

console.log(pencil);
console.log(quantidade)
console.log(preco)
console.log(imposto)
console.log(precoFinal)

// no nosso JS ao criarmos nomes de variáveis vamos sempre lembrar que elas são case sensitive
// posso usar variáveis quantas vezes quisermos e precissarmos dentro da nossa aplicação
// outra forma que temos de criar variáveis é usando let no lugar de var (é mais moderno)