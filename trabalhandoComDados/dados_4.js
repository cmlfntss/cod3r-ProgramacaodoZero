const a = 3;
//a = a + 10;

console.log(a)

// para deixarmos o valor atribuido a uma variável CONSTANTE(que não pode ser mudada), usamos a palavra const, exemplo:

//console.log(a)

// caso seja necessário criar uma variável que não vai mudar, priorize constantes e as priorize!!!! 
